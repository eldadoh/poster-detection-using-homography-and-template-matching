"""
    Rotem , 
    all the relevant data is in 'Data' dir , 
    I changed all paths and hard coded things to relative paths 
    so you can run it immediately.

    for Debug/visualizations change show Param to True on 'detect_all' function 
    and run with debugger. 

    # detect_all(planogram_images, realogram_images, show=False)

    also note the global TH of 0.3
    and INPUT_IMG desired height of 800 pixels.
"""
